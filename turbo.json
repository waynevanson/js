{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/"]
    },
    "release": {
      "cache": false,
      "dependsOn": ["^build", "^release"]
    }
  }
}
